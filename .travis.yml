language: go
sudo: false

os:
  - linux
  - osx
  
matrix:
  include:
    - go: "1.15"

before_install:
  - go get github.com/mattn/goveralls
  
script:
  - go mod vendor
  - go test
  - bin/goveralls -service=travis-ci 
