coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 1%
    patch:
      default:
        target: 90%

comment:
  layout: "reach, diff, files"
  require_changes: true

ignore:
  - "resource"
